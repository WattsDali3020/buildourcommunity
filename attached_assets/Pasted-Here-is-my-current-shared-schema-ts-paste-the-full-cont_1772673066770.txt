Here is my current shared/schema.ts:
[paste the full contents of shared/schema.ts here]
Without modifying anything that already exists, add the following to this file:

Expand the existing userRole enum by adding these values: contractor, realtor, attorney, engineer, architect, lender, inspector, appraiser
Add these new tables:

professionalProfiles — id (varchar PK UUID), userId (FK → users), licenseNumber, licenseState, licenseType, licenseExpiry (timestamp), isLicenseVerified (boolean default false), licenseVerifiedAt (timestamp), insuranceProvider, insurancePolicyNumber, insuranceExpiry (timestamp), isInsuranceVerified (boolean default false), bondingAmount (numeric), isBonded (boolean default false), specialties (text array), serviceCounties (text array), serviceStates (text array), bio (text), companyName, websiteUrl, portfolioUrls (text array), reputationScore (integer default 0), completedProjects (integer default 0), activeProjectCount (integer default 0), totalTokensEarned (numeric default 0), status (text default 'pending' — values: pending, verified, suspended, rejected), verifiedAt (timestamp), createdAt, updatedAt
professionalEndorsements — id (varchar PK UUID), professionalId (FK → professionalProfiles), userId (FK → users), comment (text optional), createdAt
projectProfessionalMatches — id (varchar PK UUID), offeringId (FK → tokenOfferings), professionalId (FK → professionalProfiles), roleNeeded (text — contractor, attorney, realtor, engineer, architect), status (text default 'invited' — values: invited, interested, proposed, selected, rejected), proposalDetails (text), proposedAmount (numeric), tokenAllocationPercent (numeric), tokenAllocationAmount (numeric), invitedAt (timestamp), respondedAt (timestamp), selectedAt (timestamp), createdAt
professionalServiceAreas — id (varchar PK UUID), professionalId (FK → professionalProfiles), county (text), state (text), zipCode (text), projectTypes (text array), isActive (boolean default true), createdAt
agentTasks — id (serial PK), agentType (text — values: property_sourcing, owner_outreach, grant_research, contractor_sourcing, realtor_outreach, legal_compliance, investor_relations, community_engagement), status (text default 'queued' — values: queued, running, completed, failed, needs_human), priority (integer default 3), inputData (jsonb), outputData (jsonb), relatedPropertyId (text), relatedOfferingId (text), relatedUserId (text), errorMessage (text), scheduledFor (timestamp), startedAt (timestamp), completedAt (timestamp), createdAt (timestamp default now())
reputationEvents — id (serial PK), professionalId (FK → professionalProfiles), eventType (text — values: project_completed, on_time_delivery, community_rating, token_earned, dispute_filed, dispute_resolved), pointsDelta (integer), description (text), relatedProjectId (text), createdAt (timestamp default now())
Follow the exact same patterns already used in this file for defining tables, enums, and types. Export Zod insert/select schemas for each new table the same way existing tables are exported. Do not touch any existing table, enum, or constant. After making the changes run npm run db:push.